{namespace bitbucket.internal.feature.tasks.taskableComment}

/**
 * Initial placeholder for a task list that will be replaced by a real task
 * list once it has been added to the DOM.
 */
{template .taskListPlaceholder}
    <ul class="task-list">
    </ul>
{/template}