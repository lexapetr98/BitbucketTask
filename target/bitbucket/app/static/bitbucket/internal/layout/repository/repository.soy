{namespace bitbucket.internal.layout}

/**
 */
{template .cloneDialogOptions}
    {foreach $webPanel in getWebPanels('bitbucket.web.repository.clone.dialog.options')}
        {$webPanel}
    {/foreach}
{/template}
