/* This only exists so users of RequireJS can depend on imagesLoaded as an AMD module.
   In Stash it is a noop because web-resources handles it. */
define('bitbucket/internal/imagesloaded', function() {

    "use strict";

});
